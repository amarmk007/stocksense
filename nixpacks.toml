[phases.install]
cmds = ["cd backend/StockSense.API && dotnet restore"]

[phases.build]
cmds = ["cd backend/StockSense.API && dotnet publish -c Release -o /app/publish"]

[start]
cmd = "dotnet /app/publish/StockSense.API.dll"
